services:
  chucklarr:
    build:
      context: .
      args:
        CHUCKLARR_BUILD_REF: ${CHUCKLARR_BUILD_REF:-local}
    container_name: chucklarr
    ports:
      - "3333:3333"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
